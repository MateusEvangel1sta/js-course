const name = `Luiz`; // string
const number = 10; // number - int
const otherNumber = 10.5; // number - float

let studentName; // undefined -> It does not point to the place in memory.
let studentSurname = null; // null -> It does not point to the place in memory.

const boolean = true; // true or false.

console.log(
  typeof name,
  typeof number,
  typeof otherNumber,
  typeof studentName,
  typeof studentSurname,
  typeof boolean
);
